.view-sidebar
  .view-sidebar-header
    .search
      input(type="text", placeholder="Search", ng-model="search.list", ng-focus="data.searchFocus = true", ng-blur="data.searchFocus = false")
  ul.view-sidebar-list
    li.item(ng-repeat="artist in data.artists | filter:{name: search.list}", ng-click="activateItem(artist, 'artist')", ng-class="{active: artist.active}")
      a
        .item-image
          img(ng-src="{{artist.coverArtURL || '/static/img/missing-artwork.png'}}")
        .item-info
          .item-info-name {{artist.name}}
          .item-info-details
            ng-pluralize(count="artist.albums.length", when="{'1': '1 Album', 'other': '{} Albums'}")
            | ,
            ng-pluralize(count="artist.songs.length", when="{'1': '1 Song', 'other': '{} Songs'}")
.view-main
  .view-main-header
    .view-details
      .item-image
        img(ng-src="{{activeItems.artist.coverArtURL || '/static/img/missing-artwork.png'}}")
      .item-info
        .item-info-name {{activeItems.artist.name}}
        .item-info-details
          ng-pluralize(count="activeItems.artist.albums.length", when="{'1': '1 Album', 'other': '{} Albums'}")
          | ,
          ng-pluralize(count="activeItems.artist.songs.length", when="{'1': '1 Song', 'other': '{} Songs'}")
          button.button(ng-click="play()") Play All
    .search
      input(type="text", placeholder="Search", ng-model="search.grid", ng-focus="data.searchFocus = true", ng-blur="data.searchFocus = false")
  .view-main-list(ng-grid="gridOptions")
