ul.view-defaults.grid-view
  li.item(ng-repeat="(name, album) in albums")
    a
      .item-image
        img(ng-src="{{album.coverArtURL || '/static/img/missing-artwork.png'}}")
      .item-info
        .item-info-name {{name}}
        .item-info-details {{album.artist}}
